import MobileNavbar from "../components/MobileNavbar";

const crew = () => {
  return (
    <div className="flex flex-col bg-crew_Mobile bg-cover bg-center bg-fixed h-full">
      <MobileNavbar />
    </div>
  );
};

export default crew;
